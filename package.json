{
  "name": "whoseiptv-events-feed",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-feed.mjs",
    "test": "node --test tests/*.test.mjs",
    "coverage:broadcasts": "node scripts/coverage-broadcasts.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
